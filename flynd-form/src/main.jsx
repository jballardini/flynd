import React from "react";
import ReactDOM from "react-dom/client";
import FlightAlertForm from "./FlightAlertForm";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FlightAlertForm />
  </React.StrictMode>
);
